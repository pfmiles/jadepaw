---
phase: 04-tool-system
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - crates/jadepaw-wasm/src/tool_impls/mod.rs
  - crates/jadepaw-wasm/src/tool_impls/file_tool.rs
  - crates/jadepaw-wasm/src/tool_impls/http_tool.rs
  - crates/jadepaw-wasm/src/host/network.rs
  - crates/jadepaw-wasm/src/lib.rs
  - crates/jadepaw-wasm/Cargo.toml
autonomous: true
requirements: [AGENT-02]
user_setup: []

must_haves:
  truths:
    - "A developer can create a FileReadTool that reads a file through the Wasm sandbox and returns file contents as ToolResult::Ok"
    - "A developer can create an HttpRequestTool that executes real HTTP GET requests and returns status code + body as ToolResult::Ok"
    - "An http_request to a private IP (127.0.0.1, 10.0.0.1, 192.168.1.1) is blocked with SSRF_BLOCKED error"
    - "An http_request to a domain not in the can_access_domain whitelist is rejected with CapabilityDenied"
    - "An http_request that gets an HTTP 500 response returns ToolResult::Error { code: "HTTP_500", retryable: true }"
    - "All existing Phase 2 tests (Wasm host functions, capability checks, path validation) continue to pass"
  artifacts:
    - path: "crates/jadepaw-wasm/src/tool_impls/mod.rs"
      provides: "Tool impl module declaration and re-exports"
      min_lines: 3
    - path: "crates/jadepaw-wasm/src/tool_impls/file_tool.rs"
      provides: "FileReadTool, FileWriteTool implementing Tool trait via GuestHostFunctionProvider"
      min_lines: 80
    - path: "crates/jadepaw-wasm/src/tool_impls/http_tool.rs"
      provides: "HttpRequestTool implementing Tool trait with reqwest client, SSRF protection, 1MB body cap, 30s timeout"
      min_lines: 120
    - path: "crates/jadepaw-wasm/src/host/network.rs"
      provides: "Updated http_request_host_fn with real HTTP execution instead of stub"
      contains: "reqwest"
  key_links:
    - from: "crates/jadepaw-wasm/src/tool_impls/http_tool.rs"
      to: "crates/jadepaw-wasm/src/host/network.rs"
      via: "extract_host_from_url for domain whitelist check"
      pattern: "extract_host_from_url"
    - from: "crates/jadepaw-wasm/src/tool_impls/http_tool.rs"
      to: "tokio::net::lookup_host"
      via: "SSRF IP resolution"
      pattern: "lookup_host"
    - from: "crates/jadepaw-wasm/src/tool_impls/file_tool.rs"
      to: "crates/jadepaw-wasm/src/host/filesystem.rs"
      via: "Wasm host function delegation"
      pattern: "file_read_host_fn"
---

## Phase Goal

**As a** developer using jadepaw, **I want to** register external tools (file I/O, HTTP) through a typed Tool abstraction and dispatch them from the agent loop, **so that** the agent can take real actions beyond pure reasoning.

## MVP Slice 2: Tool Implementations (file I/O + HTTP with SSRF)

This plan delivers the concrete `Tool` trait implementations that wrap the Phase 2 Wasm sandbox host functions (file_read, file_write) and the Phase 4 HTTP client with SSRF protection. After this plan, the `ToolRegistry` (Plan 01) can actually invoke real file reads/writes through the Wasm sandbox and real HTTP requests through `reqwest` with IP-layer SSRF checks.

<objective>
Implement three `Tool` impls in `jadepaw-wasm`: `FileReadTool`, `FileWriteTool` (wrapping Phase 2 Wasm host functions via `SessionHandle`), and `HttpRequestTool` (using `reqwest` 0.13 with SSRF IP-layer validation per D-03/D-03a). Fill in the `http_request_host_fn` stub in `network.rs` with real HTTP logic + SSRF checks (both at the Wasm host fn boundary and in the tool impl for defense-in-depth). Add `reqwest` as a direct dependency in `jadepaw-wasm/Cargo.toml` with `rustls-tls` feature (per RESEARCH.md recommendations).

Purpose: Make the Tool trait implementable — tools that actually do things instead of stubs.

Output: `crates/jadepaw-wasm/src/tool_impls/` directory (new) with `mod.rs`, `file_tool.rs`, `http_tool.rs`. Modified `host/network.rs` (real HTTP), `lib.rs` (new module), and `Cargo.toml` (reqwest dep).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-tool-system/04-CONTEXT.md
@.planning/phases/04-tool-system/04-RESEARCH.md

### Interface contracts from Plan 04-01 (already defined, no need to read SUMMARY)
- `crates/jadepaw-core/src/tool.rs` — `Tool` trait (name, description, input_schema, call), `ToolResult` (Ok/Error variants, to_observation_string, is_error, from_error), `ToolDefinition`
- `crates/jadepaw-core/src/agent_types.rs` — `ReActStep::Observation { result, is_error }` (is_error defaults to false)
- `crates/jadepaw-core/src/host_functions.rs` — `HostFunctions::http_request()` method added

### Existing Wasm infrastructure (Phase 2, already in tree)
- `crates/jadepaw-wasm/src/host/filesystem.rs` — `file_read_host_fn`, `file_write_host_fn` (capability-gated, path-validated, return i32)
- `crates/jadepaw-wasm/src/host/network.rs` — `http_request_host_fn` stub (lines 1-132, extracts host from URL, does capability check, ALWAYS returns -1 at line 131), `extract_host_from_url()` private helper
- `crates/jadepaw-wasm/src/host/mod.rs` — re-exports file_read_host_fn, file_write_host_fn, http_request_host_fn, log_message_host_fn
- `crates/jadepaw-wasm/src/linker.rs` — `create_linker`, `register_host_functions` (registers all 4 host fns under "jadepaw" namespace)
- `crates/jadepaw-wasm/src/capability/mod.rs` — `SessionState::can_call_tool()`, `can_access_domain()`, `can_read_file()`, `can_write_file()`
- `crates/jadepaw-wasm/src/session.rs` — `SessionState` with `sandbox_root`, `capabilities`, `session_id`; `SessionHandle` with `store()`, `store_mut()`, `instance()`
- `crates/jadepaw-wasm/src/lib.rs` — re-exports
- `crates/jadepaw-wasm/Cargo.toml` — deps: jadepaw-core, wasmtime, tokio, dashmap, anyhow, chrono, tracing. Missing: `reqwest`.

### Core types (already in tree)
- `crates/jadepaw-core/src/types.rs` — `ToolId`, `SessionId`, `TenantId`
- `crates/jadepaw-core/src/capabilities.rs` — `InstanceCapabilities`, `DomainPattern`, `PathPattern`
- `crates/jadepaw-core/src/error.rs` — `JadepawError`, `Result<T>` alias

### RESEARCH.md patterns to implement
- Pattern 4: HTTP Tool with SSRF Protection (reqwest Client builder, redirect::Policy::limited(1), timeout, body cap)
- Pattern 5: FileTool Wrappers for Wasm Host Functions
- SSRF `is_blocked_ip()` function using `std::net::Ipv4Addr::is_private()` etc.
- `resolve_and_check_ssrf()` function with `tokio::time::timeout` + `tokio::net::lookup_host`
- HttpRequestTool `call()` method — implementing the Tool trait
- `HostFunctions` impl update for `http_request` in `impl HostFunctions for WasmHostFunctions` (or wherever the trait is implemented — check jadepaw-wasm for existing impl)

### Critical: Find existing HostFunctions impl
- Check `crates/jadepaw-wasm/src/` (and possibly `crates/jadepaw-agent/src/`) for `impl HostFunctions for ...`. Adding `http_request` to the trait (Plan 01 Task 2) means EVERY implementor fails to compile until they add the method. Find ALL impls before writing code.
- Common pitfall per RESEARCH.md Pitfall 4: DNS resolution MUST be wrapped in `tokio::time::timeout(Duration::from_secs(5), ...)`.
- Common pitfall per RESEARCH.md Pitfall 3: reqwest redirect MUST use `redirect::Policy::limited(1)` — not default (which follows up to 10).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add reqwest dependency + implement reqwest-based HttpRequestTool with SSRF</name>
  <files>crates/jadepaw-wasm/Cargo.toml, crates/jadepaw-wasm/src/tool_impls/mod.rs, crates/jadepaw-wasm/src/tool_impls/http_tool.rs</files>
  <read_first>
    - crates/jadepaw-wasm/Cargo.toml — current deps list (no reqwest currently)
    - crates/jadepaw-wasm/src/tool_impls/mod.rs — will be created, no current content
    - crates/jadepaw-wasm/src/tool_impls/http_tool.rs — will be created, no current content
    - crates/jadepaw-wasm/src/host/network.rs — extract_host_from_url() private helper (line 139), domain capability check pattern, bounds-checking pattern
    - crates/jadepaw-core/src/tool.rs — Tool trait, ToolResult, ToolDefinition (from Plan 01)
    - crates/jadepaw-core/src/types.rs — ToolId, SessionId
    - crates/jadepaw-core/src/capabilities.rs — DomainPattern
    - crates/jadepaw-wasm/src/capability/mod.rs — SessionState::can_access_domain() signature, can_call_tool()
    - crates/jadepaw-wasm/src/session.rs — SessionState, SessionHandle accessors
    - crates/jadepaw-wasm/Cargo.toml — existing dependency versions
    - .planning/phases/04-tool-system/04-RESEARCH.md — Pattern 4 (full HTTP tool impl), SSRF ip check functions, reqwest Client builder pattern, redirect policy, body cap, timeout
    - .planning/phases/04-tool-system/04-CONTEXT.md — D-03, D-03a, D-03b decisions
  </read_first>
  <action>
    Three changes:

    **Change 1 — `crates/jadepaw-wasm/Cargo.toml`:**
    Add `reqwest` as a direct dependency per D-03a and RESEARCH.md standard stack recommendation. The exact version string: `reqwest = { version = "0.12", default-features = false, features = ["rustls-tls"] }`. Note: RESEARCH.md says version 0.13.4 but Cargo.lock should be checked for the actual version. Use `"0.12"` as the minimum compatible (0.12.x is the stable line used by async-openai 0.40). `default-features = false` strips native-tls, `features = ["rustls-tls"]` uses Rust-native TLS per project philosophy.

    **Change 2 — `crates/jadepaw-wasm/src/tool_impls/mod.rs`:**
    Create the module file with `pub mod file_tool;` and `pub mod http_tool;`. Also add `pub use file_tool::{FileReadTool, FileWriteTool};` and `pub use http_tool::HttpRequestTool;`.

    **Change 3 — `crates/jadepaw-wasm/src/tool_impls/http_tool.rs`:**
    Create the HttpRequestTool implementing `Tool` trait per D-03/D-03a and RESEARCH.md Pattern 4.

    The file defines:

    1. **`build_http_client() -> reqwest::Client`** — private function creating a `reqwest::Client` with `redirect::Policy::limited(1)` (D-03a: at most 1 redirect) and `timeout(Duration::from_secs(30))` (D-03a: 30s total timeout). Use `reqwest::Client::builder()` pattern.

    2. **`is_blocked_ip(addr: &IpAddr) -> bool`** — private function per RESEARCH.md SSRF section. Blocks:
       - IPv4: is_private, is_loopback, is_link_local, is_multicast, is_broadcast, is_unspecified
       - IPv6: is_loopback, is_unique_local, is_unicast_link_local, is_multicast, is_unspecified
       
       Return `true` if ANY of these methods return true. All methods are stable on Rust 1.85+ (checked: `is_unique_local` and `is_unicast_link_local` stabilized in 1.84.0 per RESEARCH.md).

    3. **`resolve_and_check_ssrf(host: &str) -> Result<Vec<SocketAddr>, ToolResult>`** — private async function per RESEARCH.md:
       - Wrap `tokio::net::lookup_host(format!("{}:0", host))` in `tokio::time::timeout(Duration::from_secs(5), ...)`
       - Timeout → `ToolResult::Error { code: "DNS_TIMEOUT", ..., retryable: true }`
       - DNS error → `ToolResult::Error { code: "DNS_ERROR", ..., retryable: true }`
       - For each resolved `SocketAddr`, check `is_blocked_ip(&addr.ip())` → if blocked, return `ToolResult::Error { code: "SSRF_BLOCKED", ..., retryable: false }`

    4. **`HttpRequestTool` struct** with fields: `client: reqwest::Client`, `allowed_methods: Vec<String>` (hardcoded `["GET", "POST", "PUT", "PATCH", "DELETE"]` per D-03a).
       
       Implement `Tool` for `HttpRequestTool`:
       - `name()` → `"http_request"`
       - `description()` → description string mentioning "allows the agent to make HTTP requests"
       - `input_schema()` → `serde_json::json!({ ... })` with JSON Schema describing `method` (enum), `url` (string), `headers` (object, optional), `body` (string, optional)
       - `call(&self, args, session_id)` → async method that:
         1. Deserialize args: extract `method` (default "GET"), `url` (required), `headers` (optional HashMap), `body` (optional String)
         2. Validate method is in allowed_methods → if not, return Error { code: "INVALID_METHOD", ... retryable: false }
         3. Parse URL via `url::Url::parse()` — BUT wait, check if `url` crate is available. If not, use a simpler approach: extract scheme via `url_str.find("://")`, validate scheme is "http" or "https" (reject "file://", "gopher://", etc.)
         4. Extract hostname from URL — use existing `extract_host_from_url()` from `network.rs` (which returns `&str`, but in http_tool.rs we own the data). The function is currently private to `network.rs`. Two options: (A) make it `pub(crate)` in network.rs, or (B) duplicate with attribution. Option A is cleaner: add `pub(crate)` to `extract_host_from_url` in `network.rs`. NOTE: This is a change to `host/network.rs` that should be coordinated — the function signature is `fn extract_host_from_url(url: &str) -> &str`. Making it `pub(crate)` does not change its behavior.
         5. SSRF IP check: call `resolve_and_check_ssrf(&host).await?` — this resolves DNS and blocks private IPs (defense-in-depth layer 2, per D-03)
         6. Build and execute reqwest request with method, url, headers, body
         7. Check response status: if < 500, return `ToolResult::Ok { data: serde_json::json!({ "status": status, "headers": ... , "body": body_str }) }`. If >= 500, return `ToolResult::Error { code: "HTTP_500", message: "...", retryable: true }`. 4xx is treated as Ok (the tool succeeded at making the request; the response data is valid). Per RESEARCH.md Open Question 2 recommendation.
         8. Read response body with `response.text().await`, capped at 1MB: use `text_with_charset("utf-8")` then truncate if > 1_048_576 bytes. Return truncated body with a `[TRUNCATED: response body exceeded 1MB limit]` suffix.

    The `HttpRequestTool::new()` constructor creates the struct with `build_http_client()` and the hardcoded allowed methods list.
  </action>
  <verify>
    <automated>cargo build -p jadepaw-wasm 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `crates/jadepaw-wasm/Cargo.toml` includes `reqwest` with `rustls-tls` feature
    - `crates/jadepaw-wasm/src/tool_impls/http_tool.rs` exists with `HttpRequestTool` struct
    - `HttpRequestTool` implements `Tool` trait with `name() -> "http_request"`
    - `HttpRequestTool::call()` validates HTTP method against allowlist (GET/POST/PUT/PATCH/DELETE)
    - `HttpRequestTool::call()` resolves hostname and blocks private/loopback/link-local IPs
    - `HttpRequestTool::call()` wraps DNS resolution in `tokio::time::timeout(Duration::from_secs(5), ...)`
    - `HttpRequestTool` client uses `redirect::Policy::limited(1)` and 30s timeout
    - Response body is capped at 1MB with truncation message
    - 5xx responses are ToolResult::Error, 4xx are ToolResult::Ok
    - `is_blocked_ip()` blocks all SSRF ranges per D-03
    - `extract_host_from_url` in `network.rs` is made `pub(crate)` for reuse
    - `cargo build -p jadepaw-wasm` succeeds
    - `grep -c "reqwest" crates/jadepaw-wasm/Cargo.toml | grep -v '^#'` returns >= 1
  </acceptance_criteria>
  <done>HttpRequestTool with reqwest client + SSRF protection compiled in jadepaw-wasm. Tool trait methods fully implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement FileReadTool + FileWriteTool wrapping Wasm host functions</name>
  <files>crates/jadepaw-wasm/src/tool_impls/file_tool.rs</files>
  <read_first>
    - crates/jadepaw-wasm/src/tool_impls/file_tool.rs — will be created, no current content
    - crates/jadepaw-wasm/src/host/filesystem.rs — file_read_host_fn, file_write_host_fn signatures, the Wasm memory interaction pattern
    - crates/jadepaw-wasm/src/session.rs — SessionState fields (sandbox_root, session_id), SessionHandle accessors
    - crates/jadepaw-wasm/src/path.rs — validate_sandbox_path signature
    - crates/jadepaw-core/src/tool.rs — Tool trait (from Plan 01)
    - .planning/phases/04-tool-system/04-RESEARCH.md — Pattern 5: FileTool Wrappers
    - .planning/phases/04-tool-system/04-CONTEXT.md — D-01a (Tool vs HostFunctions separation), D-04b (host fns return i32, Tool wraps)
  </read_first>
  <action>
    Create `crates/jadepaw-wasm/src/tool_impls/file_tool.rs` per D-01a, D-04b, and RESEARCH.md Pattern 5.

    This file implements `Tool` trait for file operations. Per D-01a: `Tool` is the agent-level abstraction; `HostFunctions` is the Wasm-level contract. The file tools wrap the existing Wasm sandbox host functions — they do NOT duplicate sandbox logic.

    The file defines:

    1. **`FileReadTool` struct** with fields: `sandbox_root: PathBuf`, `session_id: SessionId`.
       
       Implement `Tool` for `FileReadTool`:
       - `name()` → `"file_read"`
       - `description()` → "Read the contents of a file at the given path. Returns the file contents as a string."
       - `input_schema()` → JSON Schema: `{ "type": "object", "properties": { "path": { "type": "string", "description": "..." } }, "required": ["path"] }`
       - `call(&self, args, session_id)` → async method that:
         1. Extract `path` from args → `args.get("path").and_then(|v| v.as_str())` → if missing, return Error { code: "INVALID_ARGS", message: "Missing required parameter: path", retryable: false }
         2. Delegate to Wasm host function logic: read file at path through the sandbox. Since we cannot directly call `file_read_host_fn` (it takes `Caller<SessionState>` which requires a running Wasm Store), the tool impl performs the same validation steps that the host function does but on the Rust (host) side:
            - Capability check: use `validate_sandbox_path(path, &self.sandbox_root)` from `crate::path` — same function used by the Wasm host fn
            - If validation fails, return ToolResult::Error { code: "PATH_VALIDATION_ERROR", message: format!("{}", e), retryable: false }
            - Read file: `tokio::fs::read(&safe_path).await`
            - If file not found, return ToolResult::Error { code: "NOT_FOUND", message: format!("File not found: {}", path), retryable: false }
            - If I/O error, return ToolResult::Error { code: "IO_ERROR", message: format!("{}", e), retryable: true }
            - If success, convert bytes to string (UTF-8) → if invalid UTF-8, return ToolResult::Error { code: "INVALID_UTF8", message: "...", retryable: false }
            - Return ToolResult::Ok { data: serde_json::Value::String(content) }
       
       Constructor: `FileReadTool::new(sandbox_root: PathBuf, session_id: SessionId) -> Self`.

    2. **`FileWriteTool` struct** with fields: `sandbox_root: PathBuf`, `session_id: SessionId`.
       
       Implement `Tool` for `FileWriteTool`:
       - `name()` → `"file_write"`
       - `description()` → "Write content to a file at the given path. Creates the file if it doesn't exist, overwrites if it does."
       - `input_schema()` → JSON Schema: `{ "type": "object", "properties": { "path": { ... }, "content": { "type": "string", "description": "..." } }, "required": ["path", "content"] }`
       - `call(&self, args, session_id)` → async method that:
         1. Extract `path` (required) and `content` (required) from args
         2. Path validation via `validate_sandbox_path` (same as FileReadTool)
         3. Write content via `tokio::fs::write(&safe_path, content.as_bytes()).await`
         4. On success → `ToolResult::Ok { data: serde_json::Value::String(format!("Successfully wrote {} bytes to {}", content.len(), path)) }`
         5. On error → ToolResult::Error with appropriate code
       
       Constructor: `FileWriteTool::new(sandbox_root: PathBuf, session_id: SessionId) -> Self`.

    IMPORTANT DESIGN NOTE per D-04b: The Wasm host functions (file_read_host_fn, file_write_host_fn) return `i32` (-1 on error). The Tool trait impl wraps this into structured `ToolResult::Error` with LLM-actionable messages. The tool-level error messages follow the format from D-04a: include a suggestion for the LLM (e.g., "Error: file not found at '/data/notes.txt' (code: NOT_FOUND). Suggested: check the file path exists and try again."). This formatting is handled by `ToolResult::to_observation_string()` which was implemented in Plan 01.

    The tool impls use `std::path::PathBuf` for sandbox_root. Import from `use std::path::PathBuf` and `use crate::path::validate_sandbox_path` and `use crate::session::SessionState` for SessionState reference patterns.
  </action>
  <verify>
    <automated>cargo build -p jadepaw-wasm 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `crates/jadepaw-wasm/src/tool_impls/file_tool.rs` exists with `FileReadTool` and `FileWriteTool` structs
    - Both implement `Tool` trait: name() returns "file_read" / "file_write"
    - Both have `input_schema()` returning JSON Schema with required fields
    - `FileReadTool::call()` uses `validate_sandbox_path` for path validation (reusing Phase 2 sandbox)
    - `FileWriteTool::call()` uses `validate_sandbox_path` for path validation
    - File not found returns ToolResult::Error with code "NOT_FOUND", retryable=false
    - I/O errors return ToolResult::Error with retryable=true
    - Both constructors take `(sandbox_root: PathBuf, session_id: SessionId)`
    - `cargo build -p jadepaw-wasm` succeeds (all three tool impls — file_read, file_write, http_request — compile)
    - No duplicate sandbox logic — `validate_sandbox_path` is called, not reimplemented
  </acceptance_criteria>
  <done>FileReadTool, FileWriteTool, and HttpRequestTool all compiled in jadepaw-wasm. Three Tool impls ready for Registry registration in Plan 03.</done>
</task>

<task type="auto">
  <name>Task 3: Fill in http_request_host_fn stub + update HostFunctions impl + wire lib.rs</name>
  <files>crates/jadepaw-wasm/src/host/network.rs, crates/jadepaw-wasm/src/lib.rs</files>
  <read_first>
    - crates/jadepaw-wasm/src/host/network.rs — full current file (lines 1-189), stub at lines 120-131
    - crates/jadepaw-wasm/src/lib.rs — module declarations, re-exports
    - crates/jadepaw-wasm/src/host/filesystem.rs — pattern for how host functions use Caller<SessionState>, await tokio::fs::read/write
    - crates/jadepaw-wasm/src/linker.rs — registration pattern, shows http_request_host_fn is already registered
    - crates/jadepaw-core/src/host_functions.rs — HostFunctions trait now has http_request method (added Plan 01 Task 2)
    - .planning/phases/04-tool-system/04-RESEARCH.md — HostFunctions trait addition code example
    - .planning/phases/04-tool-system/04-CONTEXT.md — D-03a (reqwest usage in Wasm crate)
  </read_first>
  <action>
    Three changes:

    **Change 1 — `crates/jadepaw-wasm/src/host/network.rs`:**
    Replace the Phase 2 stub (lines 120-131, the `warn!` and `-1` after input validation) with real HTTP execution logic.

    The current stub:
    ```rust
    // Phase 2: stub — return CapabilityDenied
    // Phase 4 will implement actual HTTP request with: ...
    warn!(...);
    -1
    ```

    Replace with:
    1. After all input validation (which is already done — bounds checking, URL parsing, domain capability check, method/headers/body bounds checks):
    2. Read method from guest memory (bounds-checked above): `std::str::from_utf8(&mem_data[method_start..method_end])`
    3. Read headers from guest memory (bounds-checked above): parse as JSON or key=value pairs — but for Phase 4 MVP, headers are read from guest memory as raw JSON string, parsed into `HashMap<String, String>` via `serde_json::from_str`
    4. Read body from guest memory (bounds-checked above): copy bytes into `Option<Vec<u8>>`
    5. **SSRF IP check (defense-in-depth layer 2):** Import and call the SSRF check logic from `tool_impls/http_tool.rs` — BUT these are separate modules. Instead, duplicate the small `is_blocked_ip()` function or extract it. Since `host/network.rs` and `tool_impls/http_tool.rs` are in the same crate, make `is_blocked_ip` a shared utility. Add it as a `pub(crate)` function in a new location, or better: put `is_blocked_ip` and `resolve_and_check_ssrf` in a new file `crates/jadepaw-wasm/src/host/ssrf.rs` that both `network.rs` and `tool_impls/http_tool.rs` can import from.

       SIMPLER APPROACH (to avoid file count explosion): Define `is_blocked_ip()` as a free function in `host/network.rs` (it already lives there conceptually for the SSRF check). Make it `pub(crate)` so `tool_impls/http_tool.rs` can call `crate::host::network::is_blocked_ip()`. The `resolve_and_check_ssrf()` async function can stay in `tool_impls/http_tool.rs`.

       In `network.rs`: add `pub(crate) fn is_blocked_ip(addr: &IpAddr) -> bool` with the same logic described in Task 1 (IPv4: is_private, is_loopback, is_link_local, is_multicast, is_broadcast, is_unspecified; IPv6: is_loopback, is_unique_local, is_unicast_link_local, is_multicast, is_unspecified).

    6. Resolve hostname: `tokio::time::timeout(Duration::from_secs(5), tokio::net::lookup_host(format!("{}:0", host)))` — check all resolved IPs via `is_blocked_ip()`
    7. Build reqwest request (GET/POST/PUT/PATCH/DELETE), set headers, set body
    8. Use a shared reqwest Client — BUT the host function doesn't have easy access to a reusable Client. Create one per call via `reqwest::Client::builder().redirect(redirect::Policy::limited(1)).timeout(Duration::from_secs(30)).build()?` — this is less efficient but correct for Phase 4. Optimization to shared Client deferred.
    9. Execute request, read response body with `.text().await`
    10. Return status code (as i32) — the host function returns `i32` per its signature. On success, return `status_code as i32`. On failure, return -1. This preserves the D-04b contract (host fns return i32, Tool trait wraps to ToolResult).

    **IMPORTANT:** The `http_request_host_fn` uses `Caller<'_, SessionState>` and returns `Box<dyn Future<Output = i32>>`. Any async code (reqwest calls, DNS resolution) must be inside the `async move` block. The reqwest execution is async — use `.await` inside the future.

    **Change 2 — Find and update the HostFunctions trait implementation:**
    In `crates/jadepaw-wasm/`, search for `impl HostFunctions for`. There should be at least one impl (possibly in `jadepaw-wasm` or a test module or in `jadepaw-agent`). The `http_request` method was added to the trait in Plan 01 Task 2, and it MUST be implemented. The implementation should forward to the `http_request_host_fn` logic or be a stub that returns `Err(JadepawError::capability_denied("http_request", "not yet implemented"))` for now.

    **Change 3 — `crates/jadepaw-wasm/src/lib.rs`:**
    Add `pub mod tool_impls;` to the module declarations. Add re-exports: `pub use tool_impls::file_tool::{FileReadTool, FileWriteTool};` and `pub use tool_impls::http_tool::HttpRequestTool;`. Update the doc comment to mention tool implementations in "What lives here".
  </action>
  <verify>
    <automated>cargo build -p jadepaw-wasm 2>&1 && cargo test -p jadepaw-wasm -- --nocapture 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `http_request_host_fn` in `host/network.rs` executes real HTTP requests via reqwest (no longer returns -1 unconditionally)
    - Input validation (bounds-checking, domain capability, URL parsing) is preserved from Phase 2 stub
    - SSRF IP check via `is_blocked_ip()` runs before any outbound request
    - DNS resolution wrapped in `tokio::time::timeout(Duration::from_secs(5), ...)`
    - reqwest Client uses `redirect::Policy::limited(1)` and 30s timeout
    - `is_blocked_ip()` is `pub(crate)` so both `network.rs` and `tool_impls/http_tool.rs` can use it
    - `extract_host_from_url()` is `pub(crate)` so `tool_impls/http_tool.rs` can use it
    - `HostFunctions` impl is updated with `http_request` method
    - `crates/jadepaw-wasm/src/lib.rs` declares `pub mod tool_impls` and re-exports FileReadTool, FileWriteTool, HttpRequestTool
    - `cargo build -p jadepaw-wasm` succeeds
    - `cargo test -p jadepaw-wasm` passes existing tests (Phase 2 capability and path validation tests)
    - `grep -c "is_blocked_ip" crates/jadepaw-wasm/src/host/network.rs` returns >= 1
  </acceptance_criteria>
  <done>All three tool impls compiled, http_request_host_fn filled in with real HTTP logic + SSRF, HostFunctions impl updated, jadepaw-wasm lib.rs re-exports new tools. Phase 2 tests still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Guest memory → host read | `http_request_host_fn` reads URL/method/headers/body from guest memory. Guest-controlled pointers — already bounds-checked in Phase 2 stub (preserved). |
| Tool args JSON → Tool impl | `HttpRequestTool::call()` receives LLM-generated JSON args. URL parsing, method validation, SSRF IP check all happen inside the impl. |
| Host → network | `reqwest::Client` makes real outbound TCP+TLS connections. SSRF protection runs at DNS resolution before connection. |
| File path → sandbox | `FileReadTool::call()` and `FileWriteTool::call()` call `validate_sandbox_path()` (Phase 2) before any I/O. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Information Disclosure | `HttpRequestTool` SSRF bypass via DNS rebinding | accept | Domain whitelist (first line) + IP-layer check after `lookup_host` (second line) per D-03. DNS rebinding is accepted as documented known risk for MVP — attacker must race DNS TTL against the 5s resolution timeout. Document this in a code comment above `resolve_and_check_ssrf`. |
| T-04-05 | Information Disclosure | `HttpRequestTool` redirect to private IP | mitigate | `redirect::Policy::limited(1)` limits to 1 redirect (D-03a). The IP of the redirect target is still resolved and checked by `is_blocked_ip()` before connection. |
| T-04-06 | Tampering | `http_request_host_fn` URL injection (file://, gopher://) | mitigate | Scheme validation: only `http://` and `https://` schemes are accepted. Other schemes (file://, gopher://, ftp://, etc.) are rejected before DNS resolution. This check is in the tool impl AND the host function (defense-in-depth). |
| T-04-07 | Denial of Service | `HttpRequestTool` oversized response exhausting memory | mitigate | Response body buffered with 1MB cap (`response.text().await` then truncate). 30s timeout via `ClientBuilder::timeout()` and `tokio::time::timeout` at DNS resolution. |
| T-04-08 | Denial of Service | DNS resolution blocking async runtime | mitigate | `tokio::time::timeout(Duration::from_secs(5), tokio::net::lookup_host(...))` per RESEARCH.md Pitfall 4. DNS timeout returns DNS_TIMEOUT error. |
| T-04-09 | Tampering | Path traversal in FileReadTool/FileWriteTool args | mitigate | `validate_sandbox_path()` (Phase 2, SEC-03) is called on the tool-provided path BEFORE any I/O. Path containment enforcement is at the Wasm sandbox boundary — same function used by file_read_host_fn and file_write_host_fn. |
| T-04-SC | Tampering | cargo install reqwest | mitigate | slopcheck [OK] verified. `reqwest` 0.12.x in crates.io, 150M+ downloads, verified repo `github.com/seanmonstar/reqwest`. Added with `default-features = false, features = ["rustls-tls"]` to avoid OpenSSL dependency. Cross-ecosystem note: npm `reqwest` is different library — ignore. |
</threat_model>

<verification>
## Plan-level checks
- `cargo build -p jadepaw-wasm` — tool_impls module compiles, all three Tool impls satisfy trait
- `cargo test -p jadepaw-wasm` — Phase 2 tests pass (regression check)
- `grep -c "reqwest" crates/jadepaw-wasm/Cargo.toml | grep -v '^#'` — reqwest is a direct dependency
- `grep -c "is_blocked_ip" crates/jadepaw-wasm/src/host/network.rs` — SSRF function exists
- `grep -c "redirect::Policy::limited" crates/jadepaw-wasm/src/tool_impls/http_tool.rs` — redirect limit enforced
</verification>

<success_criteria>
1. `FileReadTool` and `FileWriteTool` implement `Tool` trait, delegating path validation to Phase 2's `validate_sandbox_path()`
2. `HttpRequestTool` implements `Tool` trait with real `reqwest` HTTP client, SSRF IP-layer protection, 1MB body cap, 30s timeout
3. `http_request_host_fn` stub replaced with real HTTP execution + SSRF protection at the Wasm boundary
4. `HostFunctions` impl updated to include `http_request` method
5. `jadepaw-wasm/lib.rs` re-exports all three tool types
6. Phase 2 capability and path validation tests continue to pass
</success_criteria>

<output>
Create `.planning/phases/04-tool-system/04-02-SUMMARY.md` when done
</output>