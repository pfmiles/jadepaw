# Rustfmt configuration (D-14)
style_edition = "2024"
max_width = 100

# NOTE: group_imports and imports_granularity are unstable (nightly-only)
# and have been removed. Enable them when the project adopts nightly rustfmt
# or when they stabilize on stable Rust.